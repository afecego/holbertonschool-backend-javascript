const calculateNumber = require('./0-calcul');
const assert = require('assert');

describe('calculateNumber: ', function () {
	it('...', function () {
		result = calculateNumber(1,1);
		assert.equal(result, 2)
	});
})
